name: Keep Render Awake

on:
  schedule:
    - cron: "*/5 * * * *"   # co 5 minut
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: curl -s https://wierzchucino-bot.onrender.com/ > /dev/null
